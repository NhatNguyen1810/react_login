class Record {
    constructor(id, phoneNumber, accessCode ) {
            this.id = id;
            this.phoneNumber = phoneNumber;
            this.accessCode = accessCode;   
    }
}

module.exports = Record;